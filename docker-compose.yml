services:
  bck:
    build: .
    ports:
      - "9000:9000"
    environment:
      - NODE_ENV=production
      - PORT=9000
      - DB_PATH=/app/data/db.json
    restart: unless-stopped
    volumes:
      - bck-data:/app/data

volumes:
  bck-data:
